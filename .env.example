# API / CORS
API_ALLOWED_ORIGINS=http://localhost:5173,http://103.150.191.221:5173

# Frontend
VITE_API_BASE_URL=http://localhost:4000

# Database (Postgres metadata)
PG_USER=iot
PG_PASSWORD=iotpass
PG_DB=iot_portal
DATABASE_URL=postgresql+psycopg2://iot:iotpass@postgres:5432/iot_portal

# Auth tokens
JWT_SECRET=change-me
JWT_ALG=HS256
JWT_EXPIRES_MIN=1440

# InfluxDB 2.x (time-series telemetry)
INFLUX_URL=http://influxdb:8086
INFLUX_TOKEN=dev-token
INFLUX_ORG=iot-org
INFLUX_BUCKET=iot_telemetry

# MQTT broker (Mosquitto)
MQTT_HOST=mosquitto
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_TOPIC_PREFIX=iot

# Service tuning
INGEST_POLL_INTERVAL_SEC=2
WORKER_INTERVAL_SEC=10
SIMULATOR_DEVICE_COUNT=1
SIMULATOR_PUBLISH_INTERVAL_SEC=2
SIMULATOR_ENABLED=false
